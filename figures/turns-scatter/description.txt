Turn Count Distribution Across Qwen3-Coder-30B Benchmark and Agent Runs

This figure compares per-job turn counts for six selected runs under `results/qwen3-coder-30b`, covering three benchmarks and two agent implementations. The box-and-jitter layout makes both the central tendency and the heavy-tailed outliers visible so benchmark-agent differences in interaction length are easy to compare side by side.
